{
  "name": "bench",
  "command": "scripts/desacoplar.sh sh -c 'set -a; [ -f \"$HOME/src/telegram-coordinator/.env\" ] && . \"$HOME/src/telegram-coordinator/.env\"; [ -f \"$HOME/.config/dev-secrets.env\" ] && . \"$HOME/.config/dev-secrets.env\"; set +a; cd \"$HOME/src/foveal-vision\" || exit 1; if [ -d /mnt/bench-data ]; then BENCH_VOLUME=/mnt/bench-data; else BENCH_VOLUME=\"$HOME/bench-data\"; fi; export BENCH_VOLUME; python3 scripts/bench_fleet.py {{input}} > \"/tmp/fleet-$COORD_SESSION.log\" 2>&1; node \"$HOME/src/telegram-coordinator/scripts/notify.mjs\" \"flota de benchmark terminada. Reportes en ~/src/foveal-vision/benchmarks/, log en /tmp/fleet-$COORD_SESSION.log\"' >/dev/null 2>&1 & echo \"Flota lanzada en su propio cgroup (sobrevive a este turno Y a un reinicio del bot). Tarda decenas de minutos. Log: /tmp/fleet-$COORD_SESSION.log -- avisara al terminar, pero el log es la fuente de verdad.\"",
  "encargados": [
    "echo"
  ],
  "timeoutMs": 60000
}
